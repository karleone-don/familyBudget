# 📊 Dashboard Redesign Documentation

## Overview
Полная переделка интерфейсов дашбордов расходов для **СОЛО-режима** и **СЕМЕЙНОГО режима** согласно новым макетам.

---

## 🎯 СОЛО-РЕЖИМ (Personal Dashboard)

### Заголовок
- **Основной:** "Твои расходы"
- **Подзаголовок:** "Отслеживай личные траты и достигай целей"

### Фильтры
1. **Период** - выбор временного периода:
   - Все время (All)
   - Неделя (Last 7 days)
   - Месяц (Current month)

2. **Категория** - фильтр по категориям расходов:
   - Все категории (All)
   - Динамический список из имеющихся категорий

### Кнопки действия
```
➕ Быстрая запись расхода
📊 Полный отчет
🎯 Мои цели
```

### Диаграммы
1. **Круговая диаграмма** - "Расходы по группам"
   - Показывает распределение по 4 группам:
     - 🔴 Обязательные (Housing, Food, Transport)
     - 🟢 Необязательные (Entertainment, Shopping, Beauty)
     - 🔵 Накопления (Savings, Investments)
     - 🟠 Непредвиденные (Healthcare, Other, Uncategorized)
   - Каждая группа имеет свой цвет
   - Подпись: "Группа: ₽сумма (процент)"

2. **Гистограмма** - "Тренд расходов по дням"
   - Последние 14 дней
   - X-axis: дата (повернута на 45°)
   - Y-axis: сумма в рублях
   - Столбцы с скругленными углами

### Таблица "Твоя детальная таблица расходов"

| Колонка | Описание |
|---------|----------|
| **Что купил** | Название категории |
| **Сумма** | ₽сумма.дв |
| **%** | Процент от общих расходов |
| **Статус** | Эмодзи + текст |

#### Статусы
Рассчитываются на основе процента от общей суммы:
- 🔥 **Много!** - если > 35%
- ⚠️ **Выше нормы** - если 25-35%
- ✅ **Оплачено** - если 5-25%
- 🎯 **Мало** - если < 5%

### AI Рекомендации
- Карточки с приоритетами (высокий/средний/низкий)
- Каждая содержит:
  - Заголовок (название совета)
  - Приоритет (бейдж)
  - Описание
  - Потенциальная экономия (₽)

---

## 👨‍👩‍👧‍👦 СЕМЕЙНЫЙ РЕЖИМ (Family Dashboard)

### Заголовок
- **Основной:** "Итого расходов"
- **Подзаголовок:** "Совместный бюджет семьи: [Название семьи]"

### Фильтры
1. **Месяц** - выбор месяца/года
   - Input type="month"
   - Автоматическое обновление данных

2. **Показать графики** - чекбокс для включения/отключения диаграмм
   - По умолчанию включен (true)

### Кнопки действия
```
Добавить расход
Экспорт в Excel
Настроить категории
```

### Диаграммы (показываются если включен чекбокс)
1. **Круговая диаграмма** - "Расходы по группам"
   - Аналогично СОЛО-режиму
   - Группировка по 4 категориям
   - Цветовая схема сохранена

2. **Гистограмма** - "Тренд расходов по дням"
   - Все дни выбранного месяца
   - Столбцы с скругленными углами

### Таблица "Таблица расходов по категориям"

| Колонка | Описание |
|---------|----------|
| **Категория** | Название категории |
| **Сумма** | ₽сумма.дв |
| **%** | Процент от общего семейного бюджета |
| **Тренд** | Стрелка + процент изменения |

#### Тренды
Сравниваются расходы текущего месяца с предыдущим:
- **▲ X%** (красная стрелка, цвет #FF6B6B) - расходы увеличились на X%
- **▼ X%** (зеленая стрелка, цвет #4ECB71) - расходы снизились на X%
- **→** (серая стрелка, цвет #999) - без изменений

---

## 🎨 Цветовая схема

### Группы расходов (одинаковые для обоих режимов)
```css
"Обязательные"     → #FF6B6B (красный)
"Необязательные"   → #4ECDC4 (бирюзовый)
"Накопления"       → #45B7D1 (синий)
"Непредвиденные"   → #FFA07A (оранжевый)
```

### Основная палитра (UI элементы)
```css
Primary Gradient   → #667eea → #764ba2 (фиолетовый)
White Background   → #ffffff
Light Background   → #f0f3ff, #f8f9fc
Text Primary       → #333333
Text Secondary     → #666666
Text Tertiary      → #999999
```

---

## 📱 Технические детали

### Классификация категорий
```javascript
categoryGroups = {
  "Обязательные": [
    "Housing", "Еда", "Food", "Транспорт", "Transport", "Коммунальные", "Utilities"
  ],
  "Необязательные": [
    "Развлечения", "Entertainment", "Шоппинг", "Shopping", "Красота", "Beauty"
  ],
  "Накопления": [
    "Сбережения", "Savings", "Инвестиции", "Investments"
  ],
  "Непредвиденные": [
    "Здоровье", "Healthcare", "Другое", "Other", "Неопределенное", "Uncategorized"
  ]
}
```

### СОЛО-режим: Фильтрация
```javascript
// По периоду
selectedPeriod:
  - "all"    → все данные
  - "week"   → последние 7 дней
  - "month"  → текущий месяц

// По категории
selectedCategory:
  - "all"       → все категории
  - "[Category]" → конкретная категория
```

### СЕМЕЙНЫЙ режим: Фильтрация
```javascript
// По месяцу
selectedMonth: "YYYY-MM" (ISO 8601)

// Тренд расчет
previous_month_total = сумма расходов предыдущего месяца
current_month_total = сумма расходов текущего месяца
trend_percent = |(current - previous) / previous * 100|
```

### Структура данных (Summary)
**СОЛО:**
```javascript
{
  total: number,
  byCategory: { [category]: amount },
  byGroup: { [group]: amount },
  byDay: { [day]: amount },
  count: number
}
```

**СЕМЕЙНЫЙ:**
```javascript
{
  totalFamilyExpenses: number,
  prevTotalExpenses: number,
  byCategory: { [category]: amount },
  prevByCategory: { [category]: amount },
  byGroup: { [group]: amount },
  byDay: { [day]: amount }
}
```

---

## 🔄 API Integration

### Используемые endpoints
- `GET /api/transactions/` - личные расходы (СОЛО)
- `GET /api/families/family_transactions/` - семейные расходы (СЕМЬЯ)
- `GET /api/ai/recommendations/` - рекомендации от ИИ (оба режима)

### Структура данных транзакции
```javascript
{
  transaction_id: number,
  date: "YYYY-MM-DD",
  description: string,
  amount: number (string),
  category_name: string,
  category: { category_id, category_name },
  user: { username, user_id },
  transaction_type: "expense" | "income"
}
```

---

## 💾 State Management

### СОЛО (Solo.jsx)
```javascript
useState({
  expenses: [],           // все транзакции
  summary: {},            // итоги (см. выше)
  loading: boolean,
  error: string | null,
  recommendations: [],    // рекомендации от ИИ
  selectedPeriod: "all",  // период фильтра
  selectedCategory: "all" // категория фильтра
})
```

### СЕМЕЙНЫЙ (Family.jsx)
```javascript
useState({
  familyData: {},         // информация о семье
  expenses: [],           // расходы текущего месяца
  prevMonthExpenses: [],  // расходы предыдущего месяца
  summary: {},            // итоги (см. выше)
  loading: boolean,
  error: string | null,
  recommendations: [],    // рекомендации от ИИ
  selectedMonth: "YYYY-MM", // выбранный месяц
  showCharts: boolean     // показывать ли диаграммы
})
```

---

## 🎯 Особенности реализации

### Расчет статусов (СОЛО)
```javascript
const getStatus = (amount, categoryAmount, total) => {
  const percentage = (categoryAmount / total) * 100;
  if (percentage > 35) return { emoji: "🔥", text: "Много!" };
  if (percentage > 25) return { emoji: "⚠️", text: "Выше нормы" };
  if (percentage < 5) return { emoji: "🎯", text: "Мало" };
  return { emoji: "✅", text: "Оплачено" };
};
```

### Расчет трендов (СЕМЕЙНЫЙ)
```javascript
const getTrendArrow = (current, previous) => {
  if (current > previous) return { arrow: "▲", color: "#FF6B6B" };
  if (current < previous) return { arrow: "▼", color: "#4ECB71" };
  return { arrow: "→", color: "#999" };
};
```

### Группировка категорий
```javascript
const getGroupForCategory = (category) => {
  for (const [group, categories] of Object.entries(categoryGroups)) {
    if (categories.some(c => 
      c.toLowerCase().includes(category.toLowerCase()) || 
      category.toLowerCase().includes(c.toLowerCase())
    )) {
      return group;
    }
  }
  return "Непредвиденные";
};
```

---

## 📈 Responsive Design

### Breakpoints
- **Desktop** (> 768px): Полный макет с боковыми элементами
- **Tablet** (481-768px): Адаптивная сетка 1-2 колонки
- **Mobile** (< 480px): Одна колонка, полная ширина

### Адаптивные элементы
- Диаграммы уменьшаются на мобильных
- Таблица сжимается и становится горизонтально прокручиваемой
- Кнопки занимают полную ширину на мобильных
- Фильтры складываются в вертикальную стопку

---

## ✨ UI/UX Enhancements

### Анимации
- Slide Down (заголовки) - 0.5s ease-out
- Fade In (блоки) - 0.5-0.7s ease-out
- Slide In (элементы списков) - 0.4s ease-out
- Hover Effects (карточки, кнопки) - 0.3s ease

### Интерактивные элементы
- Кнопки меняют цвет при наведении
- Строки таблиц подсвечиваются при наведении
- Карточки поднимаются при наведении
- Гладкие переходы между состояниями

### Visual Hierarchy
- Основные заголовки: 2.8rem, градиент
- Подзаголовки: 1.1rem, серый текст
- Заголовки секций: 1.3-1.4rem, граница внизу
- Обычный текст: 1rem, темный серый
- Вспомогательный текст: 0.9rem, светло-серый

---

## 🚀 Future Enhancements

Возможные улучшения (не реализованы):
- [ ] Экспорт данных в Excel
- [ ] Настройка лимитов для категорий
- [ ] История изменений расходов
- [ ] Сравнение периодов
- [ ] Прогнозирование расходов
- [ ] Общие и личные категории в семье
- [ ] Архив закрытых месяцев
- [ ] Детальный фильтр по датам

---

## 📝 Changeset

### Files Modified
1. **Solo.jsx** - полная переделка структуры и логики
2. **Solo.css** - новые стили для диаграмм, таблиц, фильтров
3. **Family.jsx** - новая логика расчета трендов, фильтрация по месяцам
4. **Family.css** - новые стили для месячного фильтра и тренд-бейджей

### Dependencies
- `recharts` - уже установлен (v3.5.1)
- React hooks - `useState`, `useEffect`
- React Router - `useNavigate`

### Breaking Changes
- Удалены фильтры по членам семьи из Family.jsx
- Изменена структура состояния (state shape)
- Удалены неиспользуемые компоненты

---

**Версия:** 1.0  
**Дата:** December 12, 2025  
**Статус:** ✅ Полностью реализовано и протестировано
